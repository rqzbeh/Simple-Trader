# Simple-Trader - runtime dependencies
# NOTE: These are conservative, compatible version ranges; adjust pins to
# match your environment / deployment constraints.

pandas>=1.5.3,<3.0
numpy>=1.24.2,<2.0
requests>=2.28.2
feedparser>=6.0.10
python-dotenv>=0.21.0
python-dateutil>=2.8.2
pytz>=2023.3

# Monitoring / Metrics
prometheus_client>=0.16.0

# Database (Postgres) migration driver (binary wheel recommended for ease of install)
psycopg2-binary>=2.9.7

# Optional / recommended for more robust HTTP & exchange clients
httpx>=0.24.0
scikit-learn>=1.3.0
# TA-Lib is optional and improves candlestick detection - it requires the TA-Lib C library
# to be installed on the system. If your environment doesn't support the C dependency,
# remove this line or use a fallback (e.g., pandas-based heuristics).
TA-Lib>=0.4.24
# If you want to get market data directly from exchanges (optional)
# ccxt>=3.0.0

# Optional: Common crypto/market utilities (uncomment if used)
# python-binance>=1.0.16

# Telegram (we use simple HTTP requests so `requests` is sufficient)
# If you prefer a library:
# python-telegram-bot>=20.0b0

# Developer / testing tools (optional)
pytest>=7.3.2
black>=24.3.0
flake8>=6.1.0
mypy>=1.10.0
